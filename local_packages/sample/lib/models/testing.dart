import 'package:flutter/widgets.dart';

class MyTestingWidget extends StatelessWidget {
  const MyTestingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Text("My Testing Widget");
  }
}